# CI dependencies — excludes heavy ML runtimes (torch / FlagEmbedding /
# sentence-transformers / scrapling). CI uses the deterministic fake embedding
# provider against a real pgvector service, so the full store + retrieval
# wiring is exercised without GPU or multi-GB model downloads.
openai>=1.25.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
flask>=3.0.0
flask-limiter>=3.7.0
rank-bm25>=0.2.2
psycopg[binary,pool]>=3.1.18
pgvector>=0.2.5
chromadb>=0.5.0
trafilatura>=1.9.0
beautifulsoup4>=4.12.0
lxml>=5.2.0
lxml_html_clean>=0.1.1
langdetect>=1.0.9
httpx>=0.27.0
structlog>=24.1.0
tqdm>=4.66.0
pytest>=8.0.0
