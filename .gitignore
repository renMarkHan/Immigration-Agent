# .env is intentionally gitignored — do not commit API keys
.env
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.DS_Store
chroma_db/
eval/results/
*.egg-info/
dist/
build/

# Generated eval reports (regenerated by eval scripts)
eval/retrieval_report.json
eval/judge_report.json

# LaTeX build artifacts + product-intro deck (regenerated locally, not shipped)
*.aux
*.out
intro.log
intro.pdf
intro.tex
generate_intro_pdf.py
Canada_PR_Navigator_产品介绍.pdf

# Local agent skills / tooling — machine-specific, not product code.
# (.devin/skills/*/SKILL.md are already tracked; only the local symlink is ignored)
.github/skills/
.devin/skills/impeccable
